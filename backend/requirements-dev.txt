# Dev / test tooling — kept out of the runtime freeze so production installs
# stay lean. Install on top of the runtime deps:
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# pytest's own deps (pluggy, iniconfig, pygments) are resolved by pip.
pytest==9.0.3
