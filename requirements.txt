python-dotenv>=1.0.0

# UI — the app.py front-end (uses modern test-ids: stMain, stBottom,
# stChatInput). Pin to a recent line so those selectors stay valid.
streamlit>=1.40.0

# LangChain core + text splitters
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0

# Vector store (Qdrant) + Gemini bindings
langchain-qdrant>=0.2.0
qdrant-client>=1.12.0
langchain-google-genai>=2.0.0

# PDF loading (table-aware extraction)
pdfplumber>=0.11.0

# Tracing
langsmith>=0.1.0